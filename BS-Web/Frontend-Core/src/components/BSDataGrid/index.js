/**
 * BSDataGrid Components
 *
 * Export all BSDataGrid related components from this folder
 */

// Main BSDataGrid component (Server-side)
export { default as BSDataGrid } from "./BSDataGrid";
export { default } from "./BSDataGrid";

// Client-side only DataGrid component
export { default as BSDataGridClient } from "./BSDataGridClient";

// Localization utilities
export { getLocaleText } from "./locales";
